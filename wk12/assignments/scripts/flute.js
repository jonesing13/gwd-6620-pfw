let fill = 220;

function setup() {
    let cnv = createCanvas(800, 600);
    colorMode(HSB); 
    userStartAudio();
}

function draw() {
    background(fill, 100, 100);

    // draw a rectangle for a flute
    // or should I make it an image?

}
